<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shrinkify – URL Shortener</title>
  <style>
    /* Base reset */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      font-size: 100%; /* 1rem = 16px */
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #0d0d1f;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 1rem;
    }

    .container {
      background-color: #262641;
      width: 100%;
      max-width: 400px;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0, 128, 255, 0.5);
      text-align: center;
      animation: fadeIn 0.8s ease-in-out both;
    }

    h1 {
      font-size: clamp(1.5rem, 5vw, 2.5rem); /* scales between 24px and 40px */
      margin-bottom: 1.5rem;
      animation: slideIn 0.8s ease both;
    }

    label {
      display: block;
      margin-bottom: 0.5rem;
      font-size: 1rem;
      font-weight: 600;
      color: #c7c7e2;
    }

    input[type="text"] {
      width: 100%;
      padding: 0.75rem;
      margin-bottom: 1.25rem;
      border: 2px solid #333366;
      border-radius: 8px;
      background-color: #333366;
      color: #fff;
      font-size: 1rem;
      transition: border-color 0.3s;
      animation: slideIn 0.8s ease both;
    }

    input[type="text"]:focus {
      outline: none;
      border-color: #6a00ff;
    }

    button {
      width: 100%;
      padding: 0.75rem;
      font-size: 1rem;
      font-weight: 600;
      color: #0a0909;
      background-color: #aaff00;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s;
      animation: fadeIn 0.8s ease both;
    }

    button:hover {
      background-color: #4a0082;
      color: #fff;
    }

    p {
      margin-top: 1.5rem;
      font-size: 1rem;
      word-wrap: break-word;
      animation: slideIn 0.8s ease both;
    }

    a {
      color: #8cfd14;
      font-weight: 600;
      text-decoration: none;
      word-break: break-all;
    }
    a:hover {
      text-decoration: underline;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    @keyframes slideIn {
      from { opacity: 0; transform: translateX(-10px); }
      to   { opacity: 1; transform: translateX(0); }
    }

    /* Small devices */
    @media (max-width: 360px) {
      .container { padding: 1.5rem; }
      h1 { font-size: 1.75rem; }
      input, button { font-size: 0.9rem; padding: 0.6rem; }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Shrinkify</h1>
    <form action="/shorten" method="post">
      <label for="longUrl">Enter long URL:</label>
      <input type="text" id="longUrl" name="longUrl" placeholder="https://example.com/very/long/url" required>
      <button type="submit">Shorten</button>
    </form>

    <% if (shortUrl) { %>
      <p>Short URL: <a href="<%= shortUrl %>" target="_blank" rel="noopener"><%= shortUrl %></a></p>
    <% } %>
  </div>
</body>
</html>
